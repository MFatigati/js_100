// 1 see file ./age_updated.js

// 2 see ./factorial.js

/* 3 The problem is that assignment is used on line 3, rather than equality comparison,
so counter is reset to 1 at the beginning of every loop, and never has a chance to be > 2

// 4 It does not produce an error, because i is incremented in line 2; It will log numbers 1-5.

// 5 see ./random_number.js

// 6 see ./factorial_recursion.js